fun [int] myiota(int n)  = iota(n)

fun int main() = 
    let arr1  = { {3, 4}, {5, 6, 7} }           in
    let marr1 = mapall(myiota, arr1 )           in
    let q     = write(marr1)                    in 

    let arr2  = { { 100, 101 }, { 102, 103, 104, 105 } } in
    let marr2 = mapall(chr, arr2)               in
    let w = write(marr2)                        in 

      10

