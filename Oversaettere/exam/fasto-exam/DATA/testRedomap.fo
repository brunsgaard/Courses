fun bool  cond(char c) = ord(c) < 105 & 100 < ord(c) // >

fun int main() = 
    let charr = "abcdefgh"                    in

    let iarr  = map(ord, charr)               in
    let redo1 = reduce(op +, 0, iarr)         in
    let w     = write(redo1)                  in
    let q     = write(" ")                    in

    let redo2 = redomap(op +, ord, 0, charr)  in
    let w     = write(redo2)                  in

      redo1 
